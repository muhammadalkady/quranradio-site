# Quran Radio

Static landing page for [quranradio.app](https://quranradio.app), hosted free on GitHub Pages.

## Develop

It's a single `index.html` — open it in a browser to preview. Edit and push to `main` to deploy.

## Deploy

Pushing to `main` auto-publishes via GitHub Pages. Custom domain is set in `CNAME`.
